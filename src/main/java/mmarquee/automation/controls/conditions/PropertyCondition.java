package mmarquee.automation.controls.conditions;

import com.sun.jna.ptr.PointerByReference;

public class PropertyCondition extends Condition {
    public PropertyCondition(PointerByReference inCondition) {
        super(inCondition);
    }
}
